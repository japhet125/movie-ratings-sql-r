Assignement 2A and 2B share the same directory
Assignement 2A is creating table for users, movies, rating then cross join the table
analize the mean, average and matrix
SQL queries are embedded directly in the R script using DBI::dbGetQuery().
